# .github/workflows/publish-vacancies.yml
name: Publish Vacancies to Telegram

on:
  schedule:
    # 13:00, 19:00, 21:00 по Москве (UTC+3 = GMT+3)
    - cron: '0 10,16,18 * * *'  # 10:00, 16:00, 18:00 UTC = 13:00, 19:00, 21:00 MSK
  workflow_dispatch:  # Ручной запуск
  # Запуск после успешного парсинга (опционально)
  workflow_run:
    workflows: ["Parse HH Jobs"]
    types:
      - completed

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    # Запускаем только если:
    # 1. Ручной запуск (workflow_dispatch)
    # 2. По расписанию (schedule)
    # 3. После успешного парсинга (workflow_run с conclusion=success)
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Используем существующие зависимости
        pip install requests==2.31.0 supabase==1.1.1 python-dateutil==2.8.2
    
    - name: Debug - Check environment
      if: ${{ github.event_name == 'workflow_dispatch' }}
      run: |
        echo "🔧 Отладочная информация:"
        echo "Тиггер: ${{ github.event_name }}"
        echo "Рабочая директория: $(pwd)"
        echo "Содержимое scripts/:"
        ls -la scripts/
        echo ""
        echo "Python информация:"
        python --version
        pip list | grep -E "(supabase|requests)"
    
    - name: Run publisher
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
      run: |
        echo "🚀 Запуск публикатора..."
        echo "Рабочая директория: $(pwd)"
        python scripts/publisher_logic.py
    
    - name: Send notification on failure
      if: failure() && secrets.SLACK_WEBHOOK != ''
      uses: rtCamp/action-slack-notify@v2
      env:
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        SLACK_CHANNEL: alerts
        SLACK_COLOR: danger
        SLACK_TITLE: "❌ Публикация вакансий завершилась ошибкой"
        SLACK_MESSAGE: "Workflow ${{ github.workflow }} завершился с ошибкой. Проверьте логи в GitHub Actions."
        SLACK_FOOTER: "Repository: ${{ github.repository }}"
