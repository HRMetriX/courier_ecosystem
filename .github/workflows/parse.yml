name: Parse HH Jobs

on:
  schedule:
    - cron: '0 6 * * *'  # 06:00 UTC = 09:00 МСК (зима)
  workflow_dispatch:

jobs:
  parse:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v3
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4  # ИСПРАВЛЕНО!
      with:
        python-version: '3.10'
    
    - name: 📦 Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: 🚀 Run Parser
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      run: |
        echo "=== НАЧАЛО ПАРСИНГА ==="
        echo "Дата: $(date)"
        echo "Время UTC: $(date -u '+%H:%M')"
        echo "Время МСК: $(TZ='Europe/Moscow' date '+%H:%M')"
        echo "======================="
        python scripts/parser.py
    
    - name: 📊 Логирование результата
      run: |
        echo "✅ Парсинг завершен"
        echo "🔄 Следующий запуск: завтра в 06:00 UTC (09:00 МСК)"
